const arrayNames = ['Chidi', 'Mma', 'Ijeoma', 'Olisa', 'Neme'];
arrayNames.forEach((arrayNames) => { //forEach is a call back function that logs each name to the console
    console.log(arrayNames);
})